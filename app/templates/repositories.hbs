<h1>Select Repositories</h1>

<p>Here's a list of your starred repositories. Please select which ones you'd
like to analyze.</p>

{{#each repository in allRepositories}}
  <div {{bind-attr class=':repository repository.checked:checked'}}
  {{action 'toggleChecked' repository bubbles=false}}>
    {{#if repository.checked}}
      {{fa-icon 'check-square-o'}}
    {{else}}
      {{fa-icon 'square-o'}}
    {{/if}}
    <span>{{repository.name}}</span>
  </div>
{{/each}}

<div class='button-block'>
  {{link-to 'Back' 'username' class='back-btn'}}
  {{link-to 'Home' 'index' class='home-btn'}}
  {{link-to 'Next' 'results' class='next-btn'}}
</div>
